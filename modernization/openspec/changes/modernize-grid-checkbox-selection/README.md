# modernize-grid-checkbox-selection

Rebuild the legacy Grid/CheckboxSelection.aspx Web Forms demo (checkbox-column multi-row selection + paging) as an ASP.NET Core Web API + React/TypeScript frontend, genuinely browser-testable in this environment via a local mock API standing in for the .NET backend
